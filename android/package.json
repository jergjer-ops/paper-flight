{
  "name": "paper-flight-android-test",
  "version": "1.3.12-new",
  "private": true,
  "scripts": {
    "check:version": "node scripts/check-version.mjs",
    "sync": "npm run check:version && npx cap sync android",
    "build:debug": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  }
}
